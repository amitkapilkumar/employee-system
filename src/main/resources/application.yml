spring:
  application:
    name: employee-management-system
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysqldb/ems
    username: root
    password: root123
  jpa:
    show-sql: true
    properties:
      hibernate:
        ddl-auto: update
        naming-strategy : org.hibernate.cfg.ImprovedNamingStrategy
        dialect: org.hibernate.dialect.MySQL8Dialect

springdoc:
  api-docs:
    path: /api-docs